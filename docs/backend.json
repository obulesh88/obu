{
  "entities": {
    "user": {
      "title": "User",
      "description": "Represents a user in the system.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "profile": {
          "type": "object",
          "description": "User's public profile information.",
          "properties": {
            "displayName": {
              "type": "string",
              "description": "The user's display name."
            },
            "photoURL": {
              "type": "string",
              "format": "uri",
              "description": "The URL of the user's profile picture."
            }
          }
        },
        "wallet": {
          "type": "object",
          "description": "User's wallet and balance information.",
          "properties": {
            "orBalance": {
              "type": "number",
              "description": "The user's balance of OR coins."
            },
            "inrBalance": {
              "type": "number",
              "description": "The user's balance in INR."
            },
            "walletAddress": {
              "type": "string",
              "description": "The user's wallet address."
            }
          }
        }
      },
      "required": ["uid", "email", "profile", "wallet"]
    },
    "task": {
      "title": "Task",
      "description": "Represents a task a user can complete.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique ID of the task."
        },
        "title": {
          "type": "string",
          "description": "The title of the task."
        },
        "description": {
          "type": "string",
          "description": "A description of the task."
        },
        "reward": {
          "type": "number",
          "description": "The amount of OR coins awarded for completing the task."
        },
        "type": {
          "type": "string",
          "enum": ["Watch Ads", "Games", "Captcha", "Other"],
          "description": "The type of the task."
        }
      },
      "required": ["id", "title", "description", "reward", "type"]
    },
    "userTask": {
      "title": "User Task",
      "description": "Represents a user's progress on a task.",
      "type": "object",
      "properties": {
        "status": {
          "type": "string",
          "enum": ["available", "in-progress", "pending-verification", "completed", "rejected"],
          "description": "The status of the task for the user."
        },
        "evidence": {
          "type": "string",
          "description": "Evidence of task completion provided by the user."
        },
        "submittedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user submitted the task."
        },
        "completedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the task was marked as completed."
        }
      },
      "required": ["status"]
    }
  },
  "auth": {
    "providers": [
      "Email/Password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/user"
      },
      "description": "Stores user profile information."
    },
    "/tasks": {
      "schema": {
        "$ref": "#/entities/task"
      },
      "description": "Stores all available tasks."
    },
    "/users/{userId}/tasks/{taskId}": {
      "schema": {
        "$ref": "#/entities/userTask"
      },
      "description": "Tracks the status of tasks for each user."
    }
  }
}
